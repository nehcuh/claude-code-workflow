name: Harness Engineering Check

on:
  pull_request:
    paths:
      - 'CLAUDE.md'
      - 'docs/**'
      - 'scripts/verify-harness.sh'
  push:
    branches: [main, develop]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Harness verification
        run: ./scripts/verify-harness.sh

      - name: Check documentation freshness
        run: |
          # Check if CLAUDE.md references exist in docs/
          echo "📋 Documentation freshness check"
          # Add custom freshness checks here
